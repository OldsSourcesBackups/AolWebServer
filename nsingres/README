$Header$

For more documentation, consult the nsingres page
on the AOLserver wiki:

  http://panoptic.com/wiki/aolserver/nsingres

This version of nsingres is known to build with Ingres r3.

Make sure the environment variable II_SYSTEM is set to where your
Ingres is installed in order to build the module.

